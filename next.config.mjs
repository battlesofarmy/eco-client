/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'tailwindui.com', 'cdn-icons-png.flaticon.com', 'admin.regalfurniturebd.com'], // Trusted domains for external images
  },
};

export default nextConfig;
